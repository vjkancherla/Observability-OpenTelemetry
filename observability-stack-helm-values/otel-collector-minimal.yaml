mode: deployment

# Required: explicit image configuration
image:
  repository: otel/opentelemetry-collector-k8s
  tag: 0.115.1

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 200m
    memory: 256Mi

# Enable ports for receiving telemetry
ports:
  otlp:
    enabled: true
    containerPort: 4317
    servicePort: 4317
    protocol: TCP
  otlp-http:
    enabled: true
    containerPort: 4318
    servicePort: 4318
    protocol: TCP

config:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: ${env:MY_POD_IP}:4317
        http:
          endpoint: ${env:MY_POD_IP}:4318
  
  processors:
    batch:
      timeout: 5s
      send_batch_size: 512
    
    memory_limiter:
      check_interval: 5s
      limit_percentage: 80
  
  exporters:
    # For Step 1: Just log everything to console
    debug:
      verbosity: detailed
  
  extensions:
    health_check:
      endpoint: ${env:MY_POD_IP}:13133
  
  service:
    extensions: [health_check]
    pipelines:
      # Simple traces pipeline - just log for now
      traces:
        receivers: [otlp]
        processors: [memory_limiter, batch]
        exporters: [debug]
      
      # Simple logs pipeline - just log for now  
      logs:
        receivers: [otlp]
        processors: [memory_limiter, batch]
        exporters: [debug]

# Service configuration
service:
  type: ClusterIP